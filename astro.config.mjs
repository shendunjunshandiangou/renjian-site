// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 站点正式域名（影响 sitemap、RSS、canonical 等绝对链接）
  site: 'https://dunkangmao.com',
});
